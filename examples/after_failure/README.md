# pactf example - after failure

This example features a test which simulates failure to use the `P_AFTER_FAILURE` macro to log something, and a test which simulates passing to demonstrate the `P_AFTER_FAILURE` macro code argument does not execute.
